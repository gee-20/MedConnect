
BACKEND LOGISTICS
    in our application ihave three users, patient, doctor and admin, the main objective of our system is to ensure the possible consultation, between doctors and patient using our mobile applicatiojn, by using the different divices, wheere is ready implemented on frontend part. 
    
USER ACCOUNT
{note the login with google account, select account what to login with after successfull  by defaul user see the patient dashboard, aslo when user try login in system with the invalid credential, the erro message credential is invalid, also login without creating the account, the erro message user not found please create the account first and login againg, possiblity for user to login again when have account,   the sytem must allow only the authenticated user can usev the system so as to ensure the security o the systen}
   user create the account using the same interface by sellecting  the role in our system, as when the patient , can create account, login using the credential used durin the creation of account and also using the google ccount , and after login see the patient dashboard and the user profile must changed, show exatly the details of user loged in our system (this perform also to doctor as well as admin can do the same like patient the way is creating the account, as when the user(doctor) select the role doctor creating the account where email must have letter (doctor), can able to login using their credential placed when is creating the account, and doctor can not logged in the system using the google account  when is try this the system direct it to patient dashboard by default, and also the profile must be seen exactly to the user logged in the system, ans user can avble to edit the profile and other issues, the user select the role admin, in account creation the email has required to have the words ifm in any email and able to login in using the credential used when creating the account), USER any of the can able to forgot the password,by creating the new and the password change in the system , and once want to login again can able to loginusing the new password , once is login with the old password again the error message password is not valid, 

DOCTOR